!
! FRRouting configuration file
!
!
!  OSPF CONFIGURATION
! ricordati che il costo in ospf è in uscita !
interface eth0
ospf cost 90
!
router ospf
! Speak OSPF on all interfaces falling in the listed subnets
network 10.0.0.0/16 area 0.0.0.0
network 100.0.0.0/30 area 1.1.1.1
network 110.0.0.0/30 area 2.2.2.2
area 1.1.1.1 stub
area 2.2.2.2 stub
!
!
log file /var/log/frr/frr.log

! notiamo che b1  di esempio si trova al confine tra tre aree.
!per questo motivo abbiamo annunciato tutte e tre le aree. Specificando quelle che
! non sono backbone come stub.



!qui sotto un esempio di router a metà fra due aree: una rip e una ospf
!router ospf
!    network 10.0.0.0/8 area 0.0.0.0
!    redistribute rip questo comando distribuisce le rotte che conosce tramite rip
!	redistribute connected
!
!router rip
!    network 11.0.0.0/8
!    route 1.0.0.0/24  
!   route 10.0.0.0/8
! queste due sono rottte che troviamo nell area ospf ma non in rip
!
!log file /var/log/frr/frr.log
